{
  "routes": [
    {
      "route": "/api-rewrite",
      "rewrite": "/api/auth-test"
    },
    {
      "route": "/protected",
      "allowedRoles": ["authenticated"]
    }
  ],
  "navigationFallback": {
    "rewrite": "/api/auth-test"
  }
}
